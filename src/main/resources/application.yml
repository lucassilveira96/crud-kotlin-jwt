spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/crud_jwt
    username: root
    password:

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none
    generate-ddl: true

server:
    port: 9090

jwt:
  secret: littlesecret